import { Component } from '@angular/core';

/** Main class representing the entire project. */
@Component({
  selector: 'app-root',
  templateUrl: './app.component.html'
})
export class AppComponent {
  /** Application title. */
  title = 'news-aggregator';
}
